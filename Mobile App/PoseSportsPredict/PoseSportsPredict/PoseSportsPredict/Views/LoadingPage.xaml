<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="PoseSportsPredict.Views.LoadingPage"
             xmlns:viewmodels="clr-namespace:PoseSportsPredict.ViewModels"
             BackgroundColor="White">
    <ContentPage.Content>
        <Grid x:DataType="viewmodels:LoadingViewModel"
              RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="2.0*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Image
                Grid.RowSpan="3"
                Aspect="AspectFit"
                HorizontalOptions="CenterAndExpand"
                WidthRequest="314"
                Source="img_poseidon_logo.png">
            </Image>
            <ActivityIndicator
                x:Name="_loadingIndicator"
                Grid.Row="2"
                Color="{StaticResource PrimaryColor}"
                IsRunning="{Binding IsLoaded, Converter={StaticResource inverseBoolConverter}}"
                HorizontalOptions="Center"
                VerticalOptions="Start">
            </ActivityIndicator>
        </Grid>
    </ContentPage.Content>
</ContentPage>