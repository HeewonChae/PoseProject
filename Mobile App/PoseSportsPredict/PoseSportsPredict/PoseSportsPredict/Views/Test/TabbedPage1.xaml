<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:test="clr-namespace:PoseSportsPredict.Views.Test"
             xmlns:controls="clr-namespace:PoseSportsPredict.Logics.Common"
             mc:Ignorable="d"
             x:Class="PoseSportsPredict.Views.Test.TabbedPage1"
            Title="TabbedPage"
            SelectedTabColor="White"
            UnselectedTabColor="LightGray">

    <TabbedPage.ToolbarItems>
        <ToolbarItem Text="Add" Command="{Binding AddItemCommand}" />
    </TabbedPage.ToolbarItems>

    <Shell.SearchHandler>
        <controls:ShellSearchHandler
            Placeholder="Enter search term"
            ShowsResults="True"
            SearchBoxVisibility="Collapsible">
            <controls:ShellSearchHandler.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.15*" />
                            <ColumnDefinition Width="0.85*" />
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="0"
                               Text="{Binding Text}"
                               FontAttributes="Bold" />
                        <Label Grid.Column="1"
                               Text="{Binding Description}" />
                    </Grid>
                </DataTemplate>
            </controls:ShellSearchHandler.ItemTemplate>
        </controls:ShellSearchHandler>
    </Shell.SearchHandler>

    <!--Pages can be added as references or inline-->
    <test:ItemsPage Title="day-3" />
    <test:ItemsPage Title="day-2" />
    <test:ItemsPage  Title="yester" />
    <test:ItemsPage Title="today" x:Name="todayItems" />
    <test:ItemsPage Title="tomorrow" />
    <test:ItemsPage  Title="day+2" />
    <test:ItemsPage Title="day+3" />
</TabbedPage>